import {createTheme} from "@nextui-org/react";

const theme = createTheme({
    type: "light",
    theme: {
        colors: {
            mainBlue: '#242B52',
            mainYellow: '#FFC200'
        }
    }
});

export default theme;
